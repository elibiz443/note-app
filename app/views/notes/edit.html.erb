<main class="container">
  <%= link_to sanitize("&larr; Back to note"), note_path(@note) %>

  <div class="header">
    <h1>Edit note</h1>
  </div>

  <<%= turbo_frame_tag @note do %>
    <%= render "form", note: @note %>
  <% end %>
</main>
